import { X25519KeyPair } from "./keypair";
export default class Encrypter {
    keypair: X25519KeyPair;
    publicKey: Uint8Array;
    constructor(config: {
        keypair?: X25519KeyPair;
        publicKey?: Uint8Array;
    });
    encrypt(data: string | Uint8Array): Promise<string>;
    decrypt(data: string): Promise<Uint8Array>;
}
export { Encrypter };
