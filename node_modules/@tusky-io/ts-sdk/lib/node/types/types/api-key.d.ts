import { apiKeyStatus } from "../constants";
export declare class ApiKey {
    key: string;
    address: string;
    expiresAt: string;
    createdAt: string;
    updatedAt: string;
    status: apiKeyStatus;
    constructor(json: any);
}
