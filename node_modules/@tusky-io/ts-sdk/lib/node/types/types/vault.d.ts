import { EncryptedVaultKeyPair, Membership } from "./membership";
import { Folder } from "./folder";
import { File } from "./file-version";
import { Encryptable } from "../crypto";
export declare class Vault extends Encryptable {
    id: string;
    status: string;
    encrypted: boolean;
    createdAt: string;
    updatedAt: string;
    owner: string;
    size?: number;
    trash?: number;
    tags?: string[];
    name: string;
    description?: string;
    memberships?: Array<Membership>;
    files?: Array<File>;
    folders?: Array<Folder>;
    constructor(vaultProto: any, keys: Array<EncryptedVaultKeyPair>);
}
export type VaultCreateOptions = {
    encrypted?: boolean;
    description?: string;
    tags?: string[];
};
export type VaultUpdateOptions = {
    name?: string;
    description?: string;
    tags?: string[];
};
