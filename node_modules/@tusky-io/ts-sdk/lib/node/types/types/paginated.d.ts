import { AxiosResponse } from "axios";
export type Paginated<T> = {
    items: Array<T>;
    nextToken: string;
    errors?: Array<ErrorItem>;
};
export type ErrorItem = {
    id: string;
    error: Error;
};
export declare const isPaginated: (response: AxiosResponse) => boolean;
export declare const nextToken: (response: AxiosResponse) => string;
