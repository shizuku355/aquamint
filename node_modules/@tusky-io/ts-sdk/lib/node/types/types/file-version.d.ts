import { FileLocationOptions, FileMetadataOptions, Hooks } from "../core/file";
import { EncryptedVaultKeyPair } from ".";
import { Encryptable } from "../crypto";
export declare class File extends Encryptable {
    name: string;
    id: string;
    blobId: string;
    ref: string;
    mimeType: string;
    owner: string;
    createdAt: string;
    updatedAt: string;
    status: string;
    size: number;
    external?: boolean;
    expiresAt?: string;
    numberOfChunks?: number;
    chunkSize?: number;
    encryptedAesKey?: string;
    vaultId: string;
    parentId?: string;
    __encrypted__?: boolean;
    constructor(file: any, keys?: Array<EncryptedVaultKeyPair>);
}
export type FileUploadOptions = Hooks & FileLocationOptions & FileMetadataOptions;
export type FileDownloadOptions = Hooks & {
    path?: string;
    skipSave?: boolean;
};
