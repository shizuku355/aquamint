import { EncryptedVaultKeyPair } from ".";
import { Encryptable } from "../crypto";
export declare class Folder extends Encryptable {
    name: string;
    size: number;
    id: string;
    type: string;
    owner: string;
    createdAt: string;
    updatedAt: string;
    status: string;
    vaultId: string;
    parentId?: string;
    __encrypted__?: boolean;
    constructor(folder: any, keys?: Array<EncryptedVaultKeyPair>);
}
