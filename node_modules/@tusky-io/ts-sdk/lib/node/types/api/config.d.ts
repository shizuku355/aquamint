import { Env } from "../types";
export declare const apiConfig: (env: Env) => {
    apiUrl: string;
    cdnUrl: string;
    gqlUrl: string;
};
export interface ApiConfig {
    apiUrl: string;
    cdnUrl: string;
    gqlUrl: string;
}
