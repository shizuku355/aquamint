import { Service, ServiceConfig } from "./service";
import { EncryptedVaultKeyPair, File } from "../../types";
declare class FileService extends Service {
    name: string;
    constructor(config?: ServiceConfig);
    setVaultContextFromNodeId(fileId: string): Promise<void>;
    processFile(object: File, shouldDecrypt: boolean, keys?: EncryptedVaultKeyPair[]): Promise<File>;
    private isEncrypted;
    setName(name: string): Promise<void>;
}
export { FileService };
