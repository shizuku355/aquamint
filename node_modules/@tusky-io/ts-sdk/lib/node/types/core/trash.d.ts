import { Service, ServiceConfig } from "./service/service";
import { Folder } from "../types";
declare class TrashModule {
    protected service: Service;
    constructor(config?: ServiceConfig);
    get(): Promise<Folder>;
    empty(): Promise<Folder>;
}
export { TrashModule };
