import { FileOrFolderInfo } from "../folder";
export declare const traverse: (entry: FileSystemEntry, includeRootFolder?: boolean, skipHidden?: boolean, rootPath?: string) => Promise<FileOrFolderInfo[]>;
export type FolderSource = FileSystemEntry;
