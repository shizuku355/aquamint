import { File } from "../../types";
import { FileModule } from "../file";
import { GetOptions } from "../../types/query-options";
import { ServiceConfig } from "../service/service";
export declare const PROXY_DOWNLOAD_URL = "/api/proxy/download";
declare class WebFileModule extends FileModule {
    private sessionFile;
    constructor(config?: ServiceConfig);
    previewUrl(id: string, options?: FileDownloadOptions): Promise<string>;
    download(id: string, options?: FileDownloadOptions): Promise<void>;
    get(id: string, options?: GetOptions): Promise<File>;
    private saveAs;
    private cdnPreviewUrl;
    private inMemoryBufferUrl;
    private webWorkerStreamUrl;
    private hasServiceWorkerInstalled;
}
export type FileDownloadOptions = {
    cancel?: AbortController;
    onProgress?: (percentageProgress: number, bytesProgress: number) => void;
};
export { WebFileModule as FileModule };
