import { FileModule } from "../file";
declare class NodeFileModule extends FileModule {
    download(id: string, options?: FileDownloadOptions): Promise<string>;
}
export type FileDownloadOptions = {
    path?: string;
};
export { NodeFileModule as FileModule };
