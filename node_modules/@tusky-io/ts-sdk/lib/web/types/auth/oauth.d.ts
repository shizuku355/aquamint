import { OAuthConfig } from "../types/auth";
interface AuthProviderConfig {
    CLIENT_ID: string;
    AUTH_URL: string;
    SCOPES: string;
}
interface AuthProviderConfigType {
    Google: AuthProviderConfig;
    Twitch: AuthProviderConfig;
}
export declare const authProviderConfig: (env?: string) => AuthProviderConfigType;
declare class OAuth {
    private env;
    private redirectUri;
    private authProvider;
    private jwtClient;
    private enokiClient;
    private storage;
    private authProviderConfig;
    constructor(config: OAuthConfig);
    initOAuthFlow(): Promise<void>;
    handleOAuthCallback(): Promise<{
        address: string;
    }>;
    exchangeCodeForTokens(code: string): Promise<void>;
    createAuthorizationUrl(): Promise<string>;
    refreshTokens(): Promise<void>;
}
export { OAuth };
