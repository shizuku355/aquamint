import { Service, ServiceConfig } from "./service";
import { EncryptedVaultKeyPair, Membership } from "../../types";
declare class MembershipService extends Service {
    constructor(config?: ServiceConfig);
    setVaultContextFromMembershipId(membershipId: string): Promise<void>;
    prepareMemberKeys(publicKey: string): Promise<EncryptedVaultKeyPair[]>;
    rotateMemberKeys(publicKeys: Map<string, string>): Promise<{
        memberKeys: Map<string, EncryptedVaultKeyPair[]>;
        keypair: any;
    }>;
    processMembership(object: Membership, isOwner: boolean): Promise<Membership>;
}
export { MembershipService };
