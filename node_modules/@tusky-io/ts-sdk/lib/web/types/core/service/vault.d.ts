import { Service, ServiceConfig } from "./service";
import { EncryptedVaultKeyPair, Vault } from "../../types";
declare class VaultService extends Service {
    name: string;
    description: string;
    constructor(config?: ServiceConfig);
    setName(name: string): Promise<void>;
    setDescription(description: string): Promise<void>;
    setVaultContext(vaultId: string): Promise<void>;
    processVault(object: Vault, shouldDecrypt: boolean, keys?: EncryptedVaultKeyPair[]): Promise<Vault>;
}
export { VaultService };
