import { Service, ServiceConfig } from "./service";
import { EncryptedVaultKeyPair, Folder } from "../../types";
declare class FolderService extends Service {
    name: string;
    constructor(config?: ServiceConfig);
    setVaultContextFromNodeId(folderId: string): Promise<void>;
    processFolder(object: Folder, shouldDecrypt: boolean, keys?: EncryptedVaultKeyPair[]): Promise<Folder>;
    setName(name: string): Promise<void>;
    private isEncrypted;
}
export { FolderService };
