import { FileOrFolderInfo } from "../folder";
export declare const traverse: (folderPath: string, includeRootFolder?: boolean, skipHidden?: boolean, basePath?: string) => FileOrFolderInfo[];
export type FolderSource = string;
