import { NetworkError } from "./network-error";
export declare const throwError: (status: number, message?: string, error?: Error) => never;
export declare const retryableErrors: (typeof NetworkError)[];
